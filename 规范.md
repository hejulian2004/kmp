# Kotlin Multiplatform 项目注释与文档规范

本规范旨在统一项目中的注释风格，提升代码的可读性与可维护性，特别针对 Compose Multiplatform 环境下的主题、资源及 UI 组件进行了定制。

---

## 文件头部注释 (File Header)
每个 Kotlin 文件顶部必须包含文件头注释。

```kotlin
/**
 * @File: 文件名 (如: App.kt)
 * @Package: 包名
 * @Description: 简述文件用途
 * @Author: 作者名 (可选)
 * @Date: 创建日期 (YYYY-MM-DD)
 */
```

---

## 主题规范 (Theme)
针对 `Theme.kt` 及其相关配置的注释。

- **规范要求**：说明主题的适用范围（全应用或局部）、颜色方案切换逻辑。
- **示例**：
```kotlin
/**
 * [InstagramTheme]
 * 应用程序全局主题配置。
 * 
 * @param darkTheme 是否启用深色模式，默认跟随系统
 * @param content 需要应用此主题的可组合项
 */
@Composable
fun InstagramTheme(...) { ... }
```

---

## 颜色规范 (Colors)
针对 `Color.kt` 中颜色定义的注释。

- **规范要求**：必须注明颜色的用途（如：主色调、辅助色）。
- **示例**：
```kotlin
/** 
 * 主品牌色：Instagram 经典蓝色
 * 对应设计稿：Primary/ActionBlue
 */
val InstagramBlue = Color(0xFF0095f6)

/** 
 * 渐变色：用于用户头像外圈的快拍状态
 */
val InstagramStoryPink = Color(0xFFe1306c)
```

---

## 排版规范 (Type/Typography)
针对 `Type.kt` 中字体样式的注释。

- **规范要求**：说明样式的适用场景（如：大标题、正文、标签）。
- **示例**：
```kotlin
/**
 * 项目标准排版配置
 * 包含：
 * - titleLarge: 用于页面标题 (22sp, Bold)
 * - bodyLarge: 用于正文内容 (16sp, Normal)
 * - labelSmall: 用于底部辅助文字 (11sp, Medium)
 */
val InstagramTypography = Typography(...)
```

---

## 资源规范 (Resources)
针对多平台共享资源（Resources）的调用或配置。

- **规范要求**：说明资源的来源及适配情况。
- **示例**：
```kotlin
/**
 * 加载并显示 Compose Multiplatform 标志
 * 来源：commonMain/composeResources/drawable
 */
painterResource(Res.drawable.compose_multiplatform)
```

---

## 函数规范 (Functions)
普通逻辑函数（非 Compose）的注释。

- **规范要求**：使用 KDoc 格式，注明参数、返回值及可能的异常。
- **示例**：
```kotlin
/**
 * 生成欢迎字符串
 * 
 * @return 返回包含当前平台名称的欢迎语
 * @throws Exception 当获取平台失败时抛出
 */
fun greet(): String { ... }
```

---

## Compose 组件规范 (Compose)
针对 `@Composable` 函数的注释。

- **规范要求**：
    1. 描述 UI 组件的功能。
    2. 说明 `@Preview` 的用途。
    3. 若使用了 `Scaffold` 或核心布局，需简述结构。
- **示例**：
```kotlin
/**
 * 首页组件
 * 
 * 包含：
 * - TopAppBar: 品牌 Logo 及操作图标
 * - Content: 欢迎文案及导航触发器
 * 
 * @param navController 用于页面跳转的控制器
 */
@Composable
fun HomeScreen(navController: NavController) { ... }
```

---

## 待办与警告 (TODO & FIXME)
- **TODO**: 记录未来需要实现的功能。
- **FIXME**: 记录需要修复的问题。
- **示例**: `// TODO: 待接入后端接口获取真实数据`


